#include "game_functions.h"
void player_function()
{
	int direction;

	direction = UP;
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
}
