#include "game_functions.h"

void player_function()
{
	walk(RIGHT);
	walk(UP);
	walk(LEFT);
	walk(DOWN);
	walk(RIGHT);
	walk(UP);
	walk(LEFT);
	walk(DOWN);
	walk(RIGHT);
	walk(UP);
	walk(LEFT);
	walk(DOWN);
	walk(RIGHT);
	walk(UP);
	walk(LEFT);
	walk(DOWN);
}
